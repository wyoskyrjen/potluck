# Template only -- no real values here. This file IS committed.
#
# Copy it to .env.local and fill in the key, then restart the dev server.
# .env.local is kept out of the repo by the `*.local` rule in .gitignore.
#
# Both values come from the Supabase dashboard: Project Settings > API Keys.
# Use the "anon" / publishable key -- never the service_role / secret key, which
# bypasses Row Level Security and must not go anywhere near client-side code.

VITE_SUPABASE_URL=https://okeocqjxpeqsvqmozxgv.supabase.co
VITE_SUPABASE_ANON_KEY=paste-your-anon-key-here
